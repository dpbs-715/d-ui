# useMixConfig

> Wrapper for combining useConfigs. Essentially creates multiple useConfigs instances, with common parts extracted externally and special handling placed in each class, overriding external configurations.

## Basic Usage

<demo ssg="true" vue="hooks/useMixConfig/basic.vue" />
